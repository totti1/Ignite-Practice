import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  imageBackground: {
    flex: 4,
    alignItems: 'center'
    // alignContent: 'center',
    // justifyContent: 'center'
    // display: 'flex'
  }
})
